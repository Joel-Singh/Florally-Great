extends ../layout

block content
  h1= name
  p= description
  p Price: #[span.pale-text #{price}]
  p Number In Stock: #[span.pale-text #{numberInStock}]
  p Region:
    a(href=region.url)= " " + region.name
